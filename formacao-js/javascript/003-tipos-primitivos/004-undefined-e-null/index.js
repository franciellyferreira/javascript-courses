// Undefined = algo que não foi definido no momento
let resposta;
console.log(resposta);

// Null = Algo que veio realmente nulo
resposta = null;
console.log(resposta);

// Undefined e Null são diferentes!
console.log(undefined === null);
